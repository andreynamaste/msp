# 🤖 Подключение к ChatGPT

## ✅ Рабочий URL для ChatGPT

**Используйте SSE endpoint:**

```
https://2msp.online/sse
```

## 📝 Инструкция по подключению

### Шаг 1: Откройте ChatGPT
Перейдите на https://chatgpt.com/

### Шаг 2: Откройте настройки Connectors
1. Нажмите на иконку настроек ⚙️
2. Выберите **Settings**
3. Перейдите в раздел **Connectors**
4. Нажмите **New Connector** или **Add Connector**

### Шаг 3: Заполните форму

```
Name:          WordPress MCP
URL:           https://2msp.online/sse
Authentication: No authentication
```

**Важно:** Используйте **`/sse`** endpoint (не `/mcp`)!

### Шаг 4: Сохраните
Нажмите **Save** или **Connect**

### Шаг 5: Проверьте подключение

Попросите ChatGPT:
```
Покажи список доступных инструментов для WordPress
```

Или:
```
Создай тестовый пост на моем WordPress сайте с заголовком "Тест MCP" 
и содержимым "Это тестовый пост через ChatGPT"
```

## ⚙️ Настройка WordPress credentials

Перед использованием настройте WordPress credentials:

```bash
nano /X/mcp_sse_server.py
```

Измените строки 27-29:
```python
WORDPRESS_URL = "https://ваш-wordpress-сайт.com/"
WORDPRESS_USERNAME = "ваш-username"
WORDPRESS_PASSWORD = "ваш-application-password"
```

**Как получить Application Password:**
1. Войдите в WordPress Admin
2. **Users** → **Your Profile**
3. Прокрутите до **Application Passwords**
4. Введите имя (например, "MCP Server")
5. Нажмите **Add New Application Password**
6. **Скопируйте пароль** (показывается только один раз!)

После изменения перезапустите сервер:
```bash
sudo systemctl restart mcp-server
```

## 🔍 Проверка работы

### Проверка endpoints:
```bash
# Health check
curl https://2msp.online/health

# SSE endpoint (должен открыться и ждать)
curl https://2msp.online/sse

# Информация о сервере
curl https://2msp.online/mcp-info
```

### Проверка в ChatGPT:
После подключения попросите:
```
Покажи список последних постов на моем WordPress сайте
```

## 🆘 Troubleshooting

### ChatGPT не может подключиться
1. Проверьте URL: **`https://2msp.online/sse`**
2. Проверьте что сервер работает: `curl https://2msp.online/health`
3. Проверьте логи: `sudo journalctl -u mcp-server -f`

### Ошибка аутентификации WordPress
1. Убедитесь, что используете **Application Password**, а не обычный пароль
2. Проверьте URL WordPress (должен быть с `https://` и слэшем в конце)
3. Проверьте логи: `sudo journalctl -u mcp-server -n 50`

### Сервер не отвечает
```bash
# Проверить статус
sudo systemctl status mcp-server

# Перезапустить
sudo systemctl restart mcp-server

# Проверить порт
netstat -tlnp | grep 8000
```

## 📊 Доступные инструменты

1. **create_post** - Создать новый WordPress пост
   - Параметры: title, content, excerpt (опционально), status (publish/draft/private)

2. **update_post** - Обновить существующий пост
   - Параметры: post_id (обязательно), title, content, excerpt (опционально)

3. **get_posts** - Получить список постов
   - Параметры: per_page (1-100, по умолчанию 10), page (по умолчанию 1)

4. **delete_post** - Удалить пост
   - Параметры: post_id (обязательно)

## 🎯 Примеры использования

### Создание поста:
```
Напиши статью про искусственный интеллект на 300 слов 
и опубликуй её на моём WordPress сайте
```

### Получение списка:
```
Покажи мне последние 5 постов на моем WordPress сайте
```

### Обновление поста:
```
Обнови пост с ID 123, измени заголовок на "Новый заголовок"
```

### Удаление поста:
```
Удали пост с ID 456 на моём WordPress сайте
```

## ✅ Готово!

Теперь ChatGPT может управлять вашим WordPress сайтом через MCP!

