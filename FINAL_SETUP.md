# ✅ Готово! MCP сервер настроен на 2msp.online

## 🎉 Что сделано:

1. ✅ Видео-приложение перенесено на порт 8080 (внутренний)
2. ✅ Nginx настроен для всего домена 2msp.online
3. ✅ MCP сервер доступен на `/mcp/` и `/sse/`
4. ✅ SSL сертификат работает для всего домена
5. ✅ Нет конфликтов между приложениями

## 🌐 Ваши URL:

### Для пользователей:
- **📹 Видео-склейка:** https://2msp.online/
- **🔧 MCP Health:** https://2msp.online/mcp-health

### Для ChatGPT:
- **🤖 Рекомендуется:** https://2msp.online/mcp
- **📡 Legacy (SSE):** https://2msp.online/sse

## 🏗️ Архитектура:

```
Интернет
  ↓ HTTPS (443)
Nginx (SSL)
  ↓
  ├─ / → Видео-приложение (порт 8080)
  ├─ /mcp → MCP сервер (порт 8000)
  └─ /sse → SSE endpoint (порт 8000)
```

## ✅ Преимущества этого решения:

1. **Нет конфликтов** - приложения на разных путях
2. **Один SSL сертификат** - для всего домена
3. **Простая настройка** - не нужен поддомен
4. **Правильная архитектура** - Nginx обрабатывает SSL

## 🤖 Подключение к ChatGPT:

1. Откройте **ChatGPT**
2. **Settings** → **Connectors** → **New Connector**
3. Заполните:
   - **Name:** `WordPress MCP`
   - **URL:** `https://2msp.online/mcp`
   - **Authentication:** `No authentication`
4. **Save**

## 🧪 Проверка работы:

```bash
# Основной сайт
curl https://2msp.online/

# MCP health
curl https://2msp.online/mcp-health

# Должен вернуть:
# {"status":"healthy","service":"wordpress-mcp-sse-server"}
```

## 🔧 Управление:

### Перезапуск всех сервисов:
```bash
# Видео-приложение
pkill -f glass_design_main.py
cd /X/opt/video/
nohup python3 glass_design_main.py > app.log 2>&1 &

# MCP сервер
sudo systemctl restart mcp-server

# Nginx
sudo systemctl restart nginx
```

### Просмотр логов:
```bash
# Видео-приложение
tail -f /X/opt/video/app.log

# MCP сервер
sudo journalctl -u mcp-server -f

# Nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

## 📊 Структура портов:

| Порт | Приложение | Доступ |
|------|-----------|--------|
| 443  | Nginx (SSL) | Публичный |
| 8080 | Видео-приложение | Локальный (через Nginx) |
| 8000 | MCP сервер | Локальный (через Nginx) |

## 🆘 Troubleshooting:

### 502 Bad Gateway
```bash
# Проверьте приложения
ps aux | grep -E 'glass_design_main|mcp_sse_server'

# Перезапустите
sudo systemctl restart mcp-server
pkill -f glass_design_main.py
cd /X/opt/video/ && nohup python3 glass_design_main.py > app.log 2>&1 &
```

### Nginx не работает
```bash
sudo nginx -t
sudo systemctl status nginx
sudo systemctl restart nginx
```

### Проверка конфигурации
```bash
sudo cat /etc/nginx/sites-available/2msp.online
```

## 🎯 Итог:

**Всё работает на одном домене без конфликтов!**

- ✅ Видео-склейка: https://2msp.online/
- ✅ MCP сервер: https://2msp.online/mcp
- ✅ Один SSL сертификат
- ✅ Правильная архитектура

